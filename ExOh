def ExOh(str)

  # code goes here
  oc = str.scan(/o/).count
  xc = str.scan(/x/).count
  
  if oc == xc 
    re = true
  else
    re = false
  end
  
  return re 
         
end
   
# keep this function call here 
# to see how to enter arguments in Ruby scroll down   
ExOh(STDIN.gets)    
