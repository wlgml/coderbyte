def TimeConvert(num)
  num2 = num.to_i
  a= num2 / 60 
  b= num2 % 60
  sum = a.to_s+":"+b.to_s
  return sum
         
end
   
# keep this function call here 
# to see how to enter arguments in Ruby scroll down   
TimeConvert(STDIN.gets)           
